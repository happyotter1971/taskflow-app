apiVersion: v1
kind: Service
metadata:
  name: taskflow-backend
  namespace: happyotter-dev
  labels:
    app: taskflow
    component: backend
spec:
  selector:
    app: taskflow
    component: backend
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP